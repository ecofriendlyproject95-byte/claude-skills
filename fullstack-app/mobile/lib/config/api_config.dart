class ApiConfig {
  // Android emulator → 10.0.2.2 maps to your machine's localhost
  // iOS simulator  → localhost
  // Physical device → your machine's local IP, e.g. 192.168.1.42
  static const String baseUrl = 'http://10.0.2.2:3000';
}
